import 'package:flutter/material.dart';

// 另一个页面的示例，需要替换成你实际的页面类
class AnotherPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('另一个页面'),
      ),
      body: Center(
        child: Text('这是另一个页面'),
      ),
    );
  }
}
